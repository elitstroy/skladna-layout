// =============================================================================
// app-v1.js — Точка входа приложения (Theme V1)
// =============================================================================
import '../styles/main-v1.scss';

document.addEventListener('DOMContentLoaded', () => {
  // глобальная инициализация
});