// =============================================================================
// app-v2.js — Точка входа приложения (Theme V2)
// =============================================================================
import '../styles/main-v2.scss';

document.addEventListener('DOMContentLoaded', () => {
  // глобальная инициализация
});